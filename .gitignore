# Datasets
*.csv
data/*.csv
dataset/*.csv

# Python
__pycache__/
*.py[cod]
*$py.class
myenv/
.venv/
venv/
env/

# Models
# Models have been deliberately tracked (requires Git LFS due to size) to ensure 
# they are built during Vercel/Render deployments. Add models/*.pkl back here if testing locally.

# Environment
.env
.DS_Store
models/*.pkl
